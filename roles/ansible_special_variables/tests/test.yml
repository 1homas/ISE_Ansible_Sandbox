---
#------------------------------------------------------------------------------
# Run tests:
#   ansible-playbook roles/ansible_special_variables/tests/test.yml
#------------------------------------------------------------------------------
- name: Test Role | ansible_special_variables
  hosts: localhost
  gather_facts: no
  vars:
  roles:
    - ansible_special_variables
