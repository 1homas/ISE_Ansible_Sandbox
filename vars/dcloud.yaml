---
#------------------------------------------------------------------------------
# Settings for Cisco dCloud Demo Environments with ISE
#------------------------------------------------------------------------------
# Tags for VPCs, VMs, and Resources
provider: dcloud
project_name: dcloud_ise_sandbox # tag all VPCs and resources

dcloud_password_default: C1sco12345
dcloud_session_id: 🛑🛑🛑🛑🛑🛑🛑🛑     # change me!
dcloud_session_owner: 🛑🛑🛑🛑🛑🛑🛑🛑  # change me!

owner: "{{ dcloud_session_owner }}"
stage: demo # ['demo','test','staging','production']

ansible_connection: ansible.netcommon.network_cli
ansible_network_os: cisco.ios.ios
ansible_ssh_user: admin # use `iseadmin` for ISE 3.2+ cloud instances
ansible_ssh_password: "{{ dcloud_password_default }}"
# ansible_ssh_private_key_file: ~/.ssh/{{ project_name }}

ise_username: admin # use `iseadmin` for ISE 3.2+ cloud instances
ise_password: ISEisC00L

ise_radius_secret: "{{ lookup('env','ISE_RADIUS_SECRET') }}" # default for RADIUS configurations
ise_tacacs_secret: "{{ lookup('env','ISE_TACACS_SECRET') }}" # default for TACACS configurations

# 
# You may use playbook vars or vars_files or role vars for patching
#
# ise_repository_name: "{{ lookup('env','ISE_REPOSITORY_NAME') }}"
# ise_patch_filename: ise-patchbundle-3.1.0.518-Patch3-22042809.SPA.x86_64.tar.gz
# ise_patch_filename: ise-patchbundle-3.1.0.518-Patch4-22091704.SPA.x86_64.tar.gz
# ise_patch_filename: ise-patchbundle-3.1.0.518-Patch7-23052004.SPA.x86_64.tar.gz
# ise_patch_filename: ise-patchbundle-3.2.0.542-Patch1-23011406.SPA.x86_64.tar.gz
# ise_patch_filename: ise-patchbundle-3.2.0.542-Patch2-23050309.SPA.x86_64.tar.gz
# ise_patch_filename: ise-patchbundle-3.2.0.542-Patch3-23071904.SPA.x86_64.tar.gz
# ise_hotpatch_filenames:
#   - ise-hotpatchbundle-3.2.0.542-Patch2-23050309.SPA.x86_64.tar.gz
